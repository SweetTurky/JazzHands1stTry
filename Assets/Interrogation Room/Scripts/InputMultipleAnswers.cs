using System.Collections;
using System.Collections.Generic;
using System.Net;
using UnityEngine;

public class InputMultipleAnswers : MonoBehaviour
{
    /*private void WitConfirmation(WitConfirm response)
    {
        if (response != null)
        {
            // Extract relevant information from the Wit response
            string intent = response.entities.ContainsKey("intent") ? response.entities["intent"][0].value : "";

            // Check the intent
            if (intent.Equals("YesIntent"))
            {
                // User confirmed with "yes", perform your Unity actions
                Debug.Log("User confirmed with 'yes'. Do something in Unity.");
                // Call a function or set a variable in Unity indicating the confirmation
            }
            else if (intent.Equals("NoIntent"))
            {
                // User said "no", handle accordingly
                Debug.Log("User said 'no'. Handle the scenario accordingly.");
            }
            else
            {
                // Handle other intents or unrecognized responses
                Debug.Log("Unrecognized response. Handle accordingly.");
            }
        }
    }*/
}
